services:
  medical-report-api:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - medical_report_storage:/app/storage
    restart: unless-stopped
volumes:
  medical_report_storage:
